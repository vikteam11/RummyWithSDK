<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <import type="android.view.View"/>
        <import type="android.graphics.Color"/>
        <variable
            name="viewModel"
            type="com.rummytitans.playcashrummyonline.cardgame.ui.onboarding.OnBoardingViewModel" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:animateLayoutChanges="true"
        android:background="@drawable/img_onboarding_bg"
        tools:context=".ui.onboarding.OnBoardingActivity">

       <ImageView
            android:id="@+id/imgBg1"
            android:layout_width="213dp"
            android:layout_height="86dp"
            android:layout_marginTop="98dp"
           app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/white"
            app:srcCompat="@drawable/new_app_logo" />


        <androidx.viewpager.widget.ViewPager
            android:id="@+id/viewPager"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginTop="28dp"
            android:clipToPadding="false"
            android:paddingHorizontal="20dp"
            android:layout_marginBottom="100dp"
            app:layout_constraintBottom_toTopOf="@id/btnLogin"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/imgBg1" />

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabs"
            android:layout_width="match_parent"
            android:layout_height="8dp"
            android:layout_gravity="fill"
            app:tabMode="scrollable"
            app:tabGravity="center"
            app:tabMaxWidth="7dp"
            android:layout_marginTop="20dp"
            app:tabIndicatorHeight="0dp"
            app:tabBackground="@drawable/tab_custom_view_selector"
            app:layout_constraintTop_toBottomOf="@+id/viewPager"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <Button
            android:id="@+id/btnLogin"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginBottom="40dp"
            android:layout_marginHorizontal="24dp"
            android:background="@drawable/bg_primary_box"
            android:backgroundTint="@color/white"
            android:fontFamily="@font/rubik_bold"
            android:text="@string/register_login"
            android:visibility="invisible"
            android:animateLayoutChanges="true"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toTopOf="@+id/checkBoxConfirm"
            android:textSize="14sp" />

        <CheckBox
            android:id="@+id/checkBoxConfirm"
            android:layout_marginStart="20dp"
            android:layout_marginTop="20dp"
            android:layout_marginEnd="20dp"
            android:layout_marginBottom="30dp"
            android:textColor="@color/white"
            app:layout_constraintEnd_toEndOf="parent"
            android:textSize="12sp"
            android:gravity="start"
            android:buttonTint="@color/white"
            android:visibility="gone"
            android:onCheckedChanged="@{(compoundButton, checked) -> viewModel.onConfirmByUser(compoundButton, checked)}"
            android:fontFamily="@font/rubik_regular"
            android:text="@string/age_confirmation_login_text"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_width="0dp"
            android:layout_height="wrap_content"/>
        <ProgressBar
            android:id="@+id/loadingLogin"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:layout_marginStart="8dp"
            android:layout_marginTop="8dp"
            android:background="@drawable/bg_white_progress"
            android:elevation="10dp"
            android:padding="@dimen/margin_min"
            android:theme="@style/ProgressBar"
            android:visibility="@{viewModel.ParentLoading?View.VISIBLE:View.GONE,default=gone}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>